<form method="post" action="{{ path('app_reservation_delete_back', {'reservid': reservation.reservid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="d-inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.reservid) }}">
    <button class="btn btn-danger btn-sm"><i class="fas fa-trash-alt"></i></button>
</form>
